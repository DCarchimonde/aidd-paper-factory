# Development smoke-test addendum

This addendum was frozen before generating or inspecting any seed-99 result.

- Seed `99` is reserved exclusively for technical pipeline validation of the new confirmatory split, clustering, unified imbalance, and downstream uncertainty scripts.
- Seed-99 results are exploratory/development outputs and must not be included in confirmatory aggregate estimates or headline manuscript claims.
- Confirmatory random and label-blind scaffold seeds remain `101` through `110`.
- Confirmatory similarity-cluster seeds remain `101` through `105`.
- Running a script successfully on seed 99 may be used to fix implementation bugs, but no method, threshold, endpoint, or outcome metric may be selected based on seed-99 numerical performance unless the resulting analysis is explicitly labeled exploratory.
