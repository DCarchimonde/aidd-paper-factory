# GitHub Setup Notes

Recommended repository name:

aidd-paper-factory

Recommended visibility:

Private before submission. Public only after manuscript submission or acceptance, depending on journal/data policy.

Basic commands:

git init
git add .
git commit -m "Initial AIDD paper factory structure"
git branch -M main
git remote add origin git@github.com:YOUR_USERNAME/aidd-paper-factory.git
git push -u origin main
